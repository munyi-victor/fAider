import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import PromptSection from '@/components/navigation/PromptSection';

const Prompt = () => {
  return (
    <View>
      <PromptSection />
    </View>
  );
}

export default Prompt;

const styles = StyleSheet.create({})